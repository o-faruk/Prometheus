# Same as requirements.txt, minus Prophet — the production containers (API + scheduler)
# never import ProphetForecaster; it's only used by run_backtest.py, which runs locally,
# not on the server. Skipping it here avoids compiling Prophet's Stan backend from source
# (CPU/RAM-heavy) in an image meant to build and run on a 1GB-RAM box.
pydantic-settings==2.7.1
psycopg[binary,pool]==3.2.4
requests==2.32.3
tenacity==9.0.0
apscheduler==3.11.0
python-dotenv==1.0.1
pandas==2.2.3
numpy==2.2.1
lightgbm==4.5.0
scikit-learn==1.6.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
